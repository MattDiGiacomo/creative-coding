function setup() {
  // create the canvas
  createCanvas(600, 800);

  // disable animation
  noLoop();
}

function draw() {

  background(255);
stroke(0);
strokeweight(2);
noFill();

function setup() {
createCanvas(400,400);
}
function draw() {
  stroke(0);
  strokeweight(2);
  for (let x = 0; x<= width; x += spacing)
  }
}
